name: Run PHPUnit

on:
  push:
    branches: [ krc/add-github-actions ]
  pull_request:

jobs:
  run_phpunit:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: your-github-token
      GITHUB_CLIENT_ID: 7d0a9a35639c58ff1b5a
      GITHUB_CLIENT_SECRET: b69992107e243c4929e2117af0305ca90e9027d9
      GITHUB_URL: http://host/auth/github/callback

    steps:
    - name: Checkout code
      uses: actions/checkout@v1

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
          php-version: 7.2
          extensions: posix, dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite, bcmath, soap, intl, gd, exif, iconv, imagick
          coverage: none
    
    - name: Install dependencies
      uses: php-actions/composer@v4
      with:
        composer_version: 1
        args: --prefer-source --no-interaction

    - name: PHP Unit
      run: vendor/bin/phpunit
